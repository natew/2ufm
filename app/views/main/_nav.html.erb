<%-
  active = { :class => 'active' }
  title ||= 'New'
-%>

<div class="title nav-container">
  <h2>Songs</h2>
  <nav class="nav-menu">
    <%= link_to 'New', '/', (title == 'New' ? active : nil) %>
    <%= link_to 'Popular', '/popular', (title == 'Popular' ? active : nil) %>
  </nav>
</div>