<%-
  icon, title, method, path =
    (action == 'add') ?
    ['k','Like this song', :post, "/broadcasts/#{id}"] :
    ['2','Unlike this song', :delete, "/broadcasts/#{id}" ]
-%>
<%= link_to count, path, class: "restricted broadcast-song #{action} pictos-heart control", title: title, remote: true, method: method %>