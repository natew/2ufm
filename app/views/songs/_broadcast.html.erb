<% icon, title, method, path = (action == 'add') ?
    ['k','Hover to like this song', :post, "/broadcasts/#{id}"] :
    ['2','Remove song from your station', :delete, "/broadcasts/#{id}" ]
%>

  <%= link_to "<strong>#{count}</strong><span></span>".html_safe, path, class: "broadcast-song #{action} control restricted tip", title: title, remote: true, method: method %>