<% size ||= :small %>
<section class="section">
  <%= link_to image_tag(artist.image.url(size)), artist, :class => 'image' %>
  <div>
    <h4><%= link_to artist.name, artist %></h4>
  </div>
</section>