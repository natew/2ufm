<div class="whole">
  <h1><%= @genre.name %></h1>
  <%= render 'genres/nav' %>

  <%= render partial: 'stations/playlist', locals: { station: @genre_station, songs: @genre_songs, classname: 'playlist-shuffle open', already_limited: true, dont_paginate: true } %>
</div>